module.exports = {
  testMatch: [
    "<rootDir>/app/**/*.test.js", // Adjust the path based on where your test files are
    "<rootDir>/app/**/*.spec.js",
  ],
  moduleFileExtensions: ["js", "jsx", "json", "node"],
};
